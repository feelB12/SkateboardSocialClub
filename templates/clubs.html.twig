{% extends "base.html.twig" %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('css/home.css') }}">{% endblock stylesheets %}

{% block main %}
    <main>
        <section class="section">
            {% for books in books %}
                <div class="card">
                    <div class="card-text">
                        {#  <h2>{{ books.title }}</h2>#}
                        {#      <img src="{{ asset('uploads/cover/'~book.coverFilename) }}" alt="Image du livre">#}
                        {#    <h3>{{ books.author }}</h3>#}
                    </div>
                    <a class="link" href="{{ path('book', {"id": books.id}) }}">En savoir plus</a>
                    <a class="link" href="{{ path('book', {"id": books.id}) }}">En savoir plus</a>
                    <a class="link" href="{{ path('book', {"id": books.id}) }}">En savoir plus</a>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock main%}